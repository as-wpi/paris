# 2026-09-03 research sequence — consolidated record

All tests pre-registered before their runs (each folder holds `preregistration.json`, `RESULTS.md` tables, `READING.md` interpretation, `all_metrics.csv`). Universe SPY, QQQ, XLK, IWM, EFA, EEM, TLT, GLD (dividend-adjusted, FMP); exposure for day T from information at close T−1; 10 bp one-way (5+3 bp in the vol-target loop); cash = T-bill; folds 2018-Q4 / 2020 / 2022 bear, 2013–14 / 2021 / 2023–24 / 2025+ bull; paired cross-fund medians with 1,000-draw bootstrap CIs.

| # | Folder | Question | Verdict |
|---|---|---|---|
| 0 | `frozen_rule_benchmark/` | Untouched PARIS regimes vs the rtl-screener's frozen S1×S2×S3 stack; four LLT features as jump inputs | Stage 1 LOSES by a mis-specified MaxDD clause, beats the stack 8/8 on Sharpe (+0.39) in substance; stage 2 four RETIREs; trend strength ≡ LLT drift |
| 0b | `calibration_memory/` | Rolling 1,260 vs expanding calibration | Both RETIRE: expanding cures anchoring, exits later in bears (trend Martin −0.17, CI excl. 0) |
| 0c | `sizing_estimator/` (E0–E3) | Range-based vol vs EWMA inside the vol-target | All RETIRE; EWMA best tracker 15/15 |
| 1 | `sizing_estimator/` (E4) | EWMA-weighted Yang–Zhang | RETIRE, 0/15 on tracking; unbiased, 13% less turnover, Sharpe wash |
| 2 | `momentum_cycle_timing/` | Four-state cycle (dynamic speeds, MED, three discrete rules) as a daily long/flat switch | All five RETIRE vs trend λ5 (Martin lost 7/8 each); dynamic speeds collapse to "ignore the fast signal" |
| 3 | `switch_conditions/` | VR(5,63) MA ≥ 1 and VIX3M−VIX ≥ 0 as conditions on the switch | Both RETIRE; VR condition is an off switch (time-in 2–5%); VIX term bleeds every bull fold |
| 4 | `hybrid_memory/` | Expanding calm centre + rolling crisis centre | Both RETIRE; sits between rolling and expanding; the expanding cure came from the crisis centre |

**Design decision D1 (approved):** the risk model is a diagnostic, not a gate; trend-on/off is the only switch; volatility enters through sizing.

**Standing stack, unchanged by the day:** trend λ=5, rolling 1,260-day calibration, monthly refit, lag 1, as the switch; EWMA λ=0.94 close-to-close vol-target (§14.1 construction, band) as the sizer. Library gained `calibration="expanding"` (default rolling) — released as 0.10.0.

**Process rules adopted:** no MaxDD gate against low-participation comparators (Martin + CI, 50% participation floor); bull-fold bleed measured relative to buy-and-hold; one feature or one parameter per test; default verdict RETIRE.

**Closed on this evidence:** LLT signals as jump inputs; four-state cycle as a daily switch; VR and VIX term structure as switch conditions; range-based volatility as the sizing estimator; calibration memory (expanding, hybrid). **Not tested, not proposed:** anything requiring a tuned threshold on these signals.
